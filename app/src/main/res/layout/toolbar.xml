<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:background="@color/md_teal_500_primary"
    android:title=""
    android:minHeight="?attr/actionBarSize"
    app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    app:logo="@mipmap/logo_uit"
    android:layout_height="wrap_content"
    style="@style/ThemeOverlay.AppCompat.ActionBar">

    <Spinner
        android:id="@+id/spinner_nav"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:dropDownVerticalOffset="@dimen/dropDownVerticalOffset"
        style="@style/Widget.AppCompat.TextView.SpinnerItem"
        android:dropDownSelector="?android:attr/listChoiceIndicatorMultiple"
        android:gravity="right|start"
        android:pointerIcon="none" />

</android.support.v7.widget.Toolbar>